- [Advantages of OSPF](#Advantages%20of%20OSPF)
- [OSPF Application Scenarios](#OSPF%20Application%20Scenarios)
- [Basic OSPF Concepts](#Basic%20OSPF%20Concepts)
	- [Router ID](#Router%20ID)
	- [Area](#Area)
	- [Metric](#Metric)
- [Three OSPF Tables](#Three%20OSPF%20Tables)
	- [OSPF Neighbor Table](#OSPF%20Neighbor%20Table)
	- [LSDB](#LSDB)
	- [OSPF Routing Table](#OSPF%20Routing%20Table)
- [Packet Format and Type](#Packet%20Format%20and%20Type)


## Advantages of OSPF

OSPF has the following advantages:
- Uses the accumulated link cost as the reference value for route selection based on the SPF algorithm.
- Transmits and receives some protocol packets in multicast mode.
- Supports area partition
- Supports load balancing among equal-cost routes.
- Supports packet authentication. 

## OSPF Application Scenarios

![[Pasted image 20260413153819.png]]

1. **Access layer**: uses transmission media such as optical fibers, twisted pairs, coaxial cables, and wireless access technologies to connect to users and allocate services and bandwidth. The access layer allows terminal users to connect to the network. Therefore, access switches have low costs and high port density.
2. **Aggregation layer**: provides policy-based connections for the access layer, such as address combination, protocol filtering, routing service, and authentication management. Network segments are divided to implement isolation, preventing network faults from spreading and affecting the core layer. The aggregation layer also provides interconnection between virtual networks at the access layer, controls and restricts access from the access layer to the core layer, and ensures security and stability of the core layer.
3. **Core layer**: implements optimized transmission between backbone networks. The core layer focuses on redundancy, reliability, and high-speed transmission.

## Basic OSPF Concepts

### Router ID

A router ID is a 32-bit integer that uniquely identifies an OSPF router in an AS.

The rules for selecting a router ID are as follows:
- The router ID of an OSPF router is manually configured (recommended).
- If the router ID is not manually configured, a router uses the largest IP address of a loopback interface as the router ID.
- If no loopback interface is configured, the router uses the largest IP address of a physical interface as the router ID. 

*To change a specified router ID, you must restart the OSPF process.*

### Area

Each OSPF area is regarded as a logical group and identified by an area ID

An OSPF area ID is a 32-bit non-negative integer in dotted decimal notation (the format is the same as that of an IPv4 address), for example, area 0.0.0.1. For simplicity, an OSPF area ID is also expressed in decimal notation.

### Metric

OSPF uses **the cost** as the route metric. Each OSPF-enabled interface maintains an interface cost. The default interface cost is **100 Mbit/s divided by interface bandwidth.** The value 100 Mbit/s is the default reference value specified by OSPF and is configurable.

![[Pasted image 20260413154404.png]]

## Three OSPF Tables

OSPF has three important tables:
- OSPF Neighbour Table
- LSDB
- OSPF RIB

### OSPF Neighbor Table

Before OSPF transmits link state information, OSPF neighbor relationships must be established.

OSPF neighbor relationships are established by exchanging *Hello packets.*

The OSPF neighbor table displays the status of the neighbor relationship between OSPF routers. You can run the *display ospf peer* command to view the status.

### LSDB

The LSDB stores the LSAs generated by R1 and received from its neighbors. In this example, the LSDB of R1 contains three LSAs.

Type indicates the LSA type, and AdvRouter indicates the router that sends the LSA.
You can run the *display ospf lsdb* command to check the LSDB.
![[Pasted image 20260413155122.png]]

### OSPF Routing Table

**The OSPF routing table and the router routing table are different**. 

An OSPF routing table contains information that guides packet forwarding, for example, destination, cost, and next hop.

*display ospf routing*

The routing table of a router is called a global routing table. Not all OSPF routes can be added to the routing table of a router.

## Packet Format and Type

OSPF defines **five types** of packets. Different types of OSPF packets have the same header format.

OSPF packets are **encapsulated in IP packets**. The protocol number in the IP header of OSPF packets is **89**.

![[Pasted image 20260413155616.png]]

Key fields: 
- Version: indicates the OSPF version. The value of this field is 2 for OSPFv2.
- Router ID: indicates the router ID of the router that generates the packet.
- Area ID: indicates the ID of the area to which the packet is advertised.
- Type: indicates the packet type.
- Packet length: indicates the length of an OSPF packet, in bytes.
- Checksum: indicates the LSA checksum. It is used to check the entire OSPF packet, including the OSPF packet header.
- Auth Type:
	- 0: no authentication
	- 1: plain-text password authentication
	- 2: cipher-text (MD5) authentication
- Authentication: indicates information required for authentication. The value of this field varies according to the value of AuType.

Подробнее: [[packet types]]

В целом про OSPF: [[network/protocols/ospf/1-content|1-content]]

#Question if the priorities of the four routers in the preceding figure are all set to 0, can OSPF work normally?
#Answer No, OSPF won't work

#Question Which types of links form an MA network by default?
#Answer **Broadcast Multi-Access** (Ethernet, FDDI and Token Ring) and **NBMA, Non-Broadcast Multi-Access** (FR and ATM)


